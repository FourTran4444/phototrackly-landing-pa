# Optional. Set to your final HTTPS origin in Vercel for canonical metadata.
NEXT_PUBLIC_SITE_URL=

# Optional, SERVER ONLY. Without this, the form saves a clearly labeled local draft.
EARLY_ACCESS_WEBHOOK_URL=
EARLY_ACCESS_WEBHOOK_TOKEN=
